@AGENTS.md

# Docs-first rule

Before generating or editing any code in this project, always check the
`/docs` directory (at the repo root) for a relevant standards document first,
and follow it. For example, `docs/ui.md` covers UI coding standards. Do not
skip this check because a task seems small or obvious.
